<div markdown="1" data-aos="fade-up">
## __4__ Built for your federation

Every federation runs athletics a little differently — licensing, age groups, national ID schemes, languages, statistics partners. OpenTrack is configured for each national federation, rather than forcing one way of working.
</div>

<div class="fed-tabs" data-aos="fade-up">
  <ul class="nav nav-pills fed-tabs__nav justify-content-md-center" id="fed-tabs-nav" role="tablist">
    {%- for fed in site.data.fed_features -%}
    <li class="nav-item" role="presentation">
      <a class="nav-link{% if forloop.first %} active{% endif %}"
         id="fed-{{ fed.code | slugify }}-tab"
         data-toggle="pill"
         href="#fed-{{ fed.code | slugify }}"
         role="tab"
         aria-controls="fed-{{ fed.code | slugify }}"
         aria-selected="{% if forloop.first %}true{% else %}false{% endif %}">
        {%- if fed.flag %}<img class="fed-tabs__flag" src="{{ site.baseurl }}/assets/img/{{ fed.flag }}" alt="" height="16">{% endif -%}
        <span>{{ fed.country }}</span>
      </a>
    </li>
    {%- endfor -%}
  </ul>

  <div class="tab-content fed-tabs__content">
    {%- for fed in site.data.fed_features -%}
    <div class="tab-pane fade{% if forloop.first %} show active{% endif %}"
         id="fed-{{ fed.code | slugify }}"
         role="tabpanel"
         aria-labelledby="fed-{{ fed.code | slugify }}-tab">
      <div class="row align-items-center">
        <div class="col-lg-6 order-lg-2 fed-tabs__media">
          {%- if fed.frame == 'tablet' -%}
          <figure class="tablet-demo">
            <img src="{{ site.baseurl }}/assets/img/{{ fed.image }}" class="screen" width="430" height="322" alt="{{ fed.country }} — {{ fed.headline }}">
            {%- include icons/ipad-black.svg width="516" height="362" class="d-block" -%}
          </figure>
          {%- elsif fed.frame == 'photo' -%}
          <figure class="fed-tabs__photo">
            <img src="{{ site.baseurl }}/assets/img/{{ fed.image }}" alt="{{ fed.country }} — {{ fed.headline }}">
          </figure>
          {%- elsif fed.frame == 'browser-wide' -%}
          <figure class="browser-demo browser-demo--wide">
            <img src="{{ site.baseurl }}/assets/img/{{ fed.image }}" class="screen" width="728" height="410" alt="{{ fed.country }} — {{ fed.headline }}">
            {%- include icons/browser-dark-wide.svg width="728" height="436" class="d-block" -%}
          </figure>
          {%- else -%}
          <figure class="browser-demo">
            <img src="{{ site.baseurl }}/assets/img/{{ fed.image }}" class="screen" width="728" height="485" alt="{{ fed.country }} — {{ fed.headline }}">
            {%- include icons/browser-dark.svg width="728" height="511" class="d-block" -%}
          </figure>
          {%- endif -%}
        </div>
        <div class="col-lg-6 order-lg-1 fed-tabs__body">
          <h3>{{ fed.headline }}</h3>
          {{ fed.blurb | markdownify }}
        </div>
      </div>
      {%- if fed.quote -%}
        {%- assign cite_name = fed.cite | split: "," | first | strip -%}
        {%- assign cite_detail = fed.cite | remove_first: cite_name | remove_first: "," | strip -%}
        {%- include testimonial.html quote=fed.quote name=cite_name detail=cite_detail flag=fed.flag -%}
      {%- endif -%}
    </div>
    {%- endfor -%}
  </div>
</div>
