<div markdown="1" data-aos="fade-up">

## __8__ OpenTrack in Action

OpenTrack allows organisers to host competitions of every size, from club opens to national championships:

</div>

<div class="row competitions" data-aos="fade-up">
{% for comp in site.data.competitions %}
  {% assign card_i = forloop.index %}
  {% if comp.images %}{% capture comp_images %}{% for img in comp.images %}{{ img | prepend: '/assets/img/' | prepend: site.baseurl }}{% unless forloop.last %}|{% endunless %}{% endfor %}{% endcapture %}{% endif %}
  <div class="col-6 col-md-4 mb-3">
    <button class="competition-card" type="button" data-name="{{ comp.name | escape }}"{% if comp.images %} data-images="{{ comp_images }}"{% else %} data-image="{{ comp.image | prepend: '/assets/img/' | prepend: site.baseurl }}"{% endif %} data-blurb="{{ comp.blurb | markdownify | escape }}"{% if comp.url %} data-url="{{ comp.url }}"{% endif %}{% if comp.logo %} data-logo="true"{% endif %}>
      <span class="competition-card__media{% if comp.logo %} competition-card__media--logo{% endif %}">
        {% if comp.images %}
        <span class="competition-card__carousel carousel slide carousel-fade" id="comp-carousel-{{ card_i }}">
          <span class="carousel-inner">
            {% for img in comp.images %}
            <span class="carousel-item{% if forloop.first %} active{% endif %}">
              <img src="{{ img | prepend: '/assets/img/' | prepend: site.baseurl }}" alt="" loading="lazy">
            </span>
            {% endfor %}
          </span>
        </span>
        {% else %}
        <img src="{{ comp.image | prepend: '/assets/img/' | prepend: site.baseurl }}" alt="" loading="lazy">
        {% endif %}
      </span>
      <span class="competition-card__name">{{ comp.name }}</span>
    </button>
  </div>
{% endfor %}
</div>

<div class="competition-modal" role="dialog" aria-modal="true" aria-labelledby="competition-modal-title">
  <div class="competition-modal__panel">
    <button class="competition-modal__close" type="button" aria-label="Close">&times;</button>
    <div class="competition-modal__media"></div>
    <div class="competition-modal__body">
      <h3 class="competition-modal__title" id="competition-modal-title"></h3>
      <div class="competition-modal__blurb"></div>
      <a class="competition-modal__link btn btn-primary btn-block" href="" target="_blank" rel="noopener" hidden>See it live</a>
    </div>
  </div>
</div>
